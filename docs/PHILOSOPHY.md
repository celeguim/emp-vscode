# EMP VS Code Philosophy

- Less is more.
- The extension is an editor.
- The CLI is the compiler.
- One command creates one resource.
- Never create dependencies automatically.
- Prefer readability over abstraction.
- If VS Code already does it well, don't reimplement it.
